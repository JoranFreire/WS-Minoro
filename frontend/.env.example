NEXT_PUBLIC_API_URL=https://api.minoro.witsense.com.br
NEXT_PUBLIC_ROUTER_URL=https://minoro.witsense.com.br
# Pagar.me public key (pk_*) — safe to expose client-side, used only to
# tokenize card data in the browser via tokenizecard.js. Leave unset to hide
# the billing/upgrade page.
NEXT_PUBLIC_PAGARME_PUBLIC_KEY=
